# Agent Context Contracts

Handoffs use structured `RequestContext` and typed context payloads for chart facts, knowledge, classical evidence, empirical evidence, intuition, prediction, validation, and response. Each includes request/subject/domain/mode, trust constraints, IDs, warnings, and dependency state. Prose is a response output, not an internal handoff contract.
