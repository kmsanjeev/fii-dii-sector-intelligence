# Calibration

`PredictionRegistry.evaluate` reports sample size and separates insufficient sample from active calibration. Future calibration groups predictions by confidence band, domain, method, and timing performance. No confidence claim is promoted until enough resolved outcomes exist.
