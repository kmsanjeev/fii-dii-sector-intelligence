# Outcome Evaluation

`OutcomeRecord` is independent of the prediction and records event type, period, direction, source, verification quality, and notes. Evaluation exposes event match, direction correctness, comparison state, and timing-error extension points. It supports direction/event/window accuracy, false positives/negatives, precision/recall, and domain-specific reporting without one misleading global score.
