# Provider Benchmark

Configured provider discovery succeeded and exposed the existing provider chain. A real 18-run OFF/ASSISTED attempt was started through `ChatEngine`, but it exceeded the controlled runtime budget without producing a stable corpus and was stopped. No stub response is presented as provider evidence. `ProviderBenchmarkRecord` preserves provider, model, prompt version, retrieval mode, chart fixture, question, answer, evidence IDs, and configuration when capture succeeds.
