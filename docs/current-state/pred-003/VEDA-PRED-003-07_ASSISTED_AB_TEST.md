# Assisted A/B Test

OFF, SHADOW, and ASSISTED are explicit runtime modes. OFF preserves normal ChatEngine behavior; SHADOW records route diagnostics; ASSISTED adds structured route/capability context while ChatEngine remains response owner. Contract probes pass for all three. A provider-backed quality winner was not established, so recommended runtime mode remains `SHADOW`.
