# D1 and D4 Evidence

The existing runtime supplies D1 houses/lords and surfaces D4 calculations in broader profiles. P015-RX corrected the D4 calculation branch to the versioned 1/4/7/10 method, but no interpretive property method is validated. P029 therefore keeps D4 calculation metadata separate from interpretation, records `D4_NOT_VALIDATED` for interpretive use, and continues conservatively through D1 facts. D4 is never allowed to override D1 by assumption. See `docs/current-state/know-prop-001/` and `docs/current-state/p015-rx/`.
