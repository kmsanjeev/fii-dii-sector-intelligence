# Performance

Bounded validation timings were network-dependent and are not promoted to a
production benchmark because all market-data probes were entitlement-blocked.
Authentication/profile and each probe completed within the bounded command
window. No polling loop, large backfill or N+1 quote pattern was introduced.
