# Negative Evidence Update

The negative register is updated as follows: POSITION_START retains
`NO_REPLICATED_SIGNAL_DETECTED_AT_CURRENT_N`; marriage retains failed
replication and retires V1; progeny remains `INSUFFICIENT_PREVALENCE` rather
than false; public-role remains ungovernable; POSEND is not yet a signal test.

These are bounded evidence states, not universal doctrinal or population-level
claims. Historical dashboards remain immutable; this document is the current
interpretation layer.
