# SILVER Corpus Freeze

Machine-readable manifest: `artifacts/07_SILVER_CORPUS_FREEZE.json`.

The corpus contains 109 verified ADB records from the current adjudication register: 32 `VERIFIED_TIER_A` and 77 `VERIFIED_TIER_B`. It is source-quality evidence for input provenance, not an independent oracle for calculated charts. No ADB raw content is committed. The manifest stores only deterministic subject-ID and source-policy hashes.

All 109 records completed through the current engine and passed the invariant checks.

