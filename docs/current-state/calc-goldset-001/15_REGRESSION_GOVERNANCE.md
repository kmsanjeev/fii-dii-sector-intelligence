# Regression Governance

- Canonical hashes exclude runtime durations and timestamps.
- JSON artifacts are generated with sorted keys and deterministic ordering.
- The production yoga detector now sorts set-derived lord combinations and yoga planet lists; this prevents Python hash-seed drift.
- The personal Dasha golden test uses `VEDA_TEST_SNAPSHOT_DATE=2026-08-10` only for that test assertion; production default remains current date.
- A second isolated `--skip-stress` build produced byte-identical JSON artifacts.
- The full stress build completed 7,022/7,022 after the correction.
- Any future expected-change update must identify the standard/version, affected component, reason, before/after hashes, and regression evidence.
- Raw ADB zip/XML and local OGDB population artifacts remain ignored and must never be staged.

