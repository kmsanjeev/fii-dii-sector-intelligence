# Performance

Deterministic local unit checks cover normalization, aggregation and Parquet
idempotency. Live/provider latency samples, batch quote latency, option-chain
latency and VEDA data-query latency are `NOT_AVAILABLE` because provider
credentials and entitlement are not configured. No artificial performance
claim is made.
