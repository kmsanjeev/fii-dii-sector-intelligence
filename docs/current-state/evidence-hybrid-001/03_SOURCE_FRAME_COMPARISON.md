# Source-Frame Comparison

The existing population does not expose occupation/frame metadata in the
source-only record, so the 40 candidates cannot honestly be stratified by
public official, scientist, athlete, institutional, entertainment or historical
frame in this run. They are recorded as
`FRAME_NOT_AVAILABLE_FROM_CURRENT_METADATA`; none can support a Tier A/B
comparison. A future stratified pilot requires richer lawful source metadata.
