# Final acceptance

Status: `PASS_WITH_CONDITION`.

The corrective parser and aggregate artifacts are deterministic, the parent regressions remain green, `ctimetype` is no longer treated as birth precision, structured accuracy/source fields are parsed, `time_unknown` and alternative-data safeguards are enforced, and no raw provider data is committed. The condition is mandatory source-note adjudication before event-corroboration expansion or any empirical qualification.

Decision: `ADB_BIRTH_PROVENANCE_ADJUDICATION`.
