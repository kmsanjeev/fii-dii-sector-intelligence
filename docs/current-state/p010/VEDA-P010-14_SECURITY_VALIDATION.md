# VEDA-P010 Security Validation

Promotion remains Admin-only.

Verified boundaries:
- research workers cannot call the promotion path directly;
- unapproved candidates cannot be promoted;
- discovery-only evidence cannot independently justify source-validated promotion;
- approved-core retrieval classification remains separate from temporary external research;
- production astrology execution remains untouched by promotion;
- rollback and supersession retain audit history.

The P010 route tests cover:
- Admin decision to `PROMOTION_READY`;
- promotion preflight;
- promotion execution;
- rollback execution.

The broader auth baseline and full regression suite remained green after P010 changes.
