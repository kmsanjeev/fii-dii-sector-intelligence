# Governed contract

Contract version: `fno-intelligence-1.0`.

The result contains `as_of_date`, `data_status`, source/schema provenance, nearest-expiry futures records, separate most-active diagnostics, options PCR subcontracts, roll/continuity flags, basis where aligned, evidence quality, and limitations. Routes are bounded and GET-only. No arbitrary path, window or raw query is accepted.
