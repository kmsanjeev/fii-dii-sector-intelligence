# Runtime smoke

A deterministic synthetic full chart selected `ASHTAKAVARGA_RAW_BPHS_PRIMARY_V2`. Result status: `RAW_SOURCE_CONTRACT_IMPLEMENTED_AND_VALIDATED`; chart-specific raw SAV total: `32`; separate chart-specific Lagna total: `3`; explicit chart-specific combined total: `35`. The source-table invariants remain planetary `336`, Lagna `49`, combined `385`. Runtime totals vary with chart positions; oracle equality, not a hard-coded per-chart total, is the correctness gate. No reduction fields were applied and contract metadata matched.
