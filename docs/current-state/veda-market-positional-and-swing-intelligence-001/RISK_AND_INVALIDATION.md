# Risk and invalidation

The contract exposes evidence conflicts, freshness, event risk, volatility and
deterministic invalidation context. Where valid price-derived levels exist,
they are contextual invalidation observations rather than stop orders. Numeric
entry prices, targets, position sizes and broker actions are intentionally not
provided by default.
