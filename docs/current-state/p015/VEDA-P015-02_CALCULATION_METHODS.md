# Calculation Methods

The governed registry mirrors the existing `KundliEngine._varga_sign` methods. D20 now routes explicitly through `D20_VIMSHAMSHA_BPHS_CATEGORY_START_V1`; the generic fallback remains available for other Vargas and for explicit legacy comparison.
