# API Compatibility

No public or internal production API was changed. The legacy
`calculate_bav()` and `calculate_sav()` signatures remain unchanged, and no
canonical metadata was added to production outputs. Compatibility validation
for a future canonical API is deferred until the contract is reconciled.
