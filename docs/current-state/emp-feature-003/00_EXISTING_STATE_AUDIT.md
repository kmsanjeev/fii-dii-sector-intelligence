# VEDA-EMP-FEATURE-003 - Existing State Audit

EMP-FEATURE-001 and EMP-FEATURE-002 closed the `POSITION_START` family with
five frozen features and replicated no-association results. Those contracts
remain immutable and are not reused as this study's target.

The existing empirical event feed contains seven `POSITION_END` observations
from four previously exposed subjects. It does not contain the preferred
independent cohort of 20 subjects. Those observations are retained only as
`LEGACY_SECONDARY`; no primary validation/holdout inference is performed.

The outcome-free POP-001 population remains the only prevalence baseline.
Production, Approved Core, RAG semantics, ML and PRED-M4 are unchanged.
