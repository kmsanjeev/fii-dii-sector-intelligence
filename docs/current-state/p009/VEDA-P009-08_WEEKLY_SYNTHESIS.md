# Weekly Synthesis

Weekly runs are represented as a distinct cadence and produce a persisted weekly digest.

The current weekly output is intentionally conservative:
- platform summary;
- run/failure counts;
- candidate and contradiction counts;
- provider health snapshot;
- backlog state.

This phase does not yet synthesize new Approved Core Knowledge. Weekly outputs remain research operations artifacts.
