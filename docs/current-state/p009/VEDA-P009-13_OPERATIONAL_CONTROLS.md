# Operational Controls

New Admin-controllable runtime operations:
- pause all research
- resume all research
- toggle kill switch
- run due schedules now
- pause/resume a domain through domain status updates
- enable/disable providers
- inspect recent digests

These controls are exposed through the existing `/api/research/*` admin surface.
