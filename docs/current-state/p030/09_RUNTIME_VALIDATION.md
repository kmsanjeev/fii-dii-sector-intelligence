# Runtime Validation

The supported Python runtime returns a non-empty `TRAVEL_RELOCATION` result with reasoning trace, D4 trust gaps, timing state, safety status and explicit dimensional outputs. Routine provider calls are zero.
