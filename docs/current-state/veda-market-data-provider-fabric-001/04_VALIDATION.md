# Validation

Focused FII result: `17 passed` covering provider fabric, intraday foundation,
API contract baseline and broker security. The API baseline was regenerated
from the canonical application after the two new provider routes and its
count/critical-endpoint assertions were updated.

Focused VEDA result: `21 passed` including provider registry, public runtime,
market capability and provider-fabric tests.

The fabric has no provider calls. Live provider access remains unvalidated;
that is a condition, not a fabricated success.
