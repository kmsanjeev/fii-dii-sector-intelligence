# Identifier Standard

IDs use `VEDA-SWW-<ENTITY>-<HUMAN_LABEL>-<HASH>` with canonical JSON inputs, SHA-256 digest material, sorted fields and no timestamps, random UUIDs, URLs or traversal order. Rebuilding the same record produces the same ID.
