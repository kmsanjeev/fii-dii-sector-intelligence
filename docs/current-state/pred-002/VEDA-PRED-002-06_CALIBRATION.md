# Calibration

Qualitative bands remain `VERY_LOW`, `LOW`, `MODERATE`, `HIGH`, and `VERY_HIGH`. The configured default minimum resolved sample is 10 per band. Calibration returns `INSUFFICIENT_SAMPLE` below that threshold; no artificial probabilities or Brier score are introduced.

Current state: `INSUFFICIENT_SAMPLE`; PRED-M4 not achieved.
