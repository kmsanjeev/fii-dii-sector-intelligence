# Orchestrator-Assisted Pilot

`VEDA_ORCHESTRATION_MODE` supports `OFF`, `SHADOW`, and `ASSISTED`. Default remains `SHADOW`. OFF omits orchestration; SHADOW records the route without changing the answer; ASSISTED adds structured route/capability context to the existing ChatEngine prompt while ChatEngine remains the final response owner. Primary orchestration is not enabled.
