# Domain Performance

The existing rebuildable domain registry remains the authoritative aggregation path. It reports predictions, resolved cases, correct/partial/incorrect states, event/direction/timing hits, false positives, sample size, and calibration state independently by domain. No global VEDA accuracy number is published.
