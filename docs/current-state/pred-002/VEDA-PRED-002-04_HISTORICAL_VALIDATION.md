# Historical Validation

The harness reuses `DurablePredictionRegistry`: audit case, freeze cutoffs, mark `HISTORICAL_CASE`, lock prediction, then append the separately supplied outcome and evaluation. It supports event, direction, timing, early/late error, and false-positive scoring.

The available repository material does not support a valid 10-case empirical pilot. This is reported as insufficient sample, not converted into a statistic.
