# Limitations

The current layer is deterministic and metadata-first. Semantic inferred reply-to and indirect pronoun resolution remain conservative. It does not implement persistent group memory, full graph inference, speaker personality profiling, multi-chart reasoning, or a group-specific response generator. Human group evaluation remains pending.
