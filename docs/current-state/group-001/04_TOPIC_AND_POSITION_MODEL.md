# Topics and Positions

The deterministic topic layer tracks a bounded topic ID/label, owner, continuation/new-topic state, and return-to-prior behavior within a transcript. Position is represented conservatively as `SUPPORTS`, `OPPOSES`, or `UNDECIDED`; changes are not silently treated as permanent personality traits.
