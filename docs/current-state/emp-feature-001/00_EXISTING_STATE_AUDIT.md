# VEDA-EMP-FEATURE-001 - Existing State Audit

The shared `CaseRegistry` is the only empirical case store. At execution it
contained 25 eligible `HISTORICAL_VERIFIED` cases across five event types.
The selected first study family is `POSITION_START`: six events from three
subjects. Existing cases are previously exposed research data; no new holdout
was acquired and no existing sealed holdout was opened.

The outcome-free `VEDA-POP-OGDB-001` population contains 1,000 charts and was
used without outcome joins. Existing P016 Vimshottari and D1 calculations,
the timing rebaseline controls, source registry, and holdout governance were
reused. No duplicate registry, case database, feature engine, ML model, RAG
store, or production rule was created.

The first feature set contains five atomic Mahadasha/natal-house relationships.
They are `PLATFORM_SYNTHESIS` research features: source lineage is recorded,
but empirical testing does not upgrade source authority or establish an event
rule.
