# Contract Audit

The existing `PredictionRecord` already supports prediction cutoff,
knowledge/data cutoffs, method/rule versions, supporting and opposing
evidence, alternatives, confidence state, lock state, versioning and immutable
outcome recording. PRED-004 reuses this contract; it does not create a second
prediction store.
