# Calibration

Qualitative bands are `VERY_LOW`, `LOW`, `MODERATE`, `HIGH`, and `VERY_HIGH`. Calibration aggregates issued, resolved, correct, partial, and incorrect records by band. The configurable default minimum sample is 10; current state is `INSUFFICIENT_SAMPLE`, so PRED-M4 is not claimed.
