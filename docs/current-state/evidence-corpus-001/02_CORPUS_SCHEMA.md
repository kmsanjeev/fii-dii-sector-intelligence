# Corpus Schema

`VEDA-LONGITUDINAL-EVIDENCE-SCHEMA-1.0` stores one subject with one birth
provenance object and multiple event objects. Event objects retain start/end,
precision, original text, normalized date only for exact DAY, normalization
method, source URLs, source tier, source cluster, corroboration, conflicts and
prior exposure. Birth and event provenance are never duplicated or conflated.
