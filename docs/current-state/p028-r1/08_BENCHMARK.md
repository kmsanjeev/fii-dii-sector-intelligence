# Benchmark

The focused deterministic benchmark defines 120 development scenarios, 20 complete method results through repeated independent inputs, cyclic/boundary cases, missing-input cases, and 40 holdout scenarios. Expected values are asserted from explicit component behavior, not benchmark-ID lookup in production code.
