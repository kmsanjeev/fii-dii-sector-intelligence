# Runtime Validation

Focused result: `37 passed` across P015-RX2, KNOW-SPIRIT, P015-RX/D4, P015 Varga governance and P031.

The supported numeric runtime path succeeds for D20; fixed and dual signs use Sagittarius and Leo starts respectively; Ascendant, planets and nodes route through the selected method; metadata exposes method ID/version and separate calculation/interpretation states.

The full Python suite was attempted with `py -3.11 -m pytest -q` and timed out after approximately 184 seconds without a completed result. It is not reported as PASS. Frontend/build was not rerun because no frontend or UI contract changed.
