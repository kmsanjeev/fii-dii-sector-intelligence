# Method Variant Decision

Legacy method preserved: `D20_LEGACY_GENERIC_VARGA_V0` for historical comparison and any explicit legacy consumer.

Governed default: `D20_VIMSHAMSHA_BPHS_CATEGORY_START_V1`.

The generic fallback remains available globally for unrelated Vargas, but governed D20 routing no longer reaches it. The selected method has `calculation_status = PARTIALLY_VALIDATED`, `mapping_status = SOURCE_MAPPING_INCOMPLETE`, and `interpretation_status = NOT_VALIDATED`.
