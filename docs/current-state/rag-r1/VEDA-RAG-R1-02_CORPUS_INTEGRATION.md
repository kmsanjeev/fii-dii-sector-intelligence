# Corpus Integration

The existing `KnowledgeEvidenceRecord` contract, research candidate store, and `UnifiedCorpusBuilder` were extended. Research-tier records retain source, claim, passage, conflict, validation, trust-zone, and version metadata. No historical store is deleted or promoted.
