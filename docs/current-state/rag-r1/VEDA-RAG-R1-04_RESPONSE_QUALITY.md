# Response Quality

Context bundles now expose selected trust zones and filtered-knowledge metadata. This makes “VEDA knows it but did not use it” observable and prevents research evidence from disappearing silently.
