# Next Implementation Recommendation

**Recommended next candidate:** `VEDA-ADM-EMP-001 — Empirical Case Intake & Bulk Import Console`.

**Why:** The current bottleneck is zero legitimate empirical cases. A governed intake workflow would enable founder/user case submission and CSV/Excel batch ingestion while reusing the existing shared case registry and provenance controls.

**Dependencies:** EMP-001, existing case registry, prediction/outcome contracts, provenance and leakage validation, admin authorization.

**Readiness:** DEFERRED, not authorized by RM-001. Before starting, verify explicit authorization, privacy/consent handling, import schema, duplicate semantics, and founder-provided sample data. P027 remains reserved and is not started.
