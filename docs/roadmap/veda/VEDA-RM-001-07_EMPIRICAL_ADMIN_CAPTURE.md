# VEDA-ADM-EMP-001 Capture

Status update: `IMPLEMENTED / FROZEN`. The original deferred roadmap item is
now implemented under `docs/current-state/adm-emp-001/`; the shared empirical
store and prediction/outcome contracts remain authoritative.

**Status:** CAPTURED / DEFERRED / NOT_IMPLEMENTED

Planned admin console capabilities: single-case entry, CSV and Excel import, templates, mapping preview, validation, row errors, duplicate/case-family detection, birth data, events, provenance, verification, cutoffs, classifications, audit trail, bulk summaries, and rejection reasons.

Workflow: `PREVIEW → VALIDATE → ACCEPT → INGEST`. It must reuse the existing shared case/prediction/outcome stores and create no parallel empirical database.
